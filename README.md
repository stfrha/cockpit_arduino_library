This is a library that is common for all cockpit controllers of my F/A18C cockpit project. It is the basis for the arduino part of the controller. Only a slight scetch is needed to build for the arduino.

The library requires the stfrha/ArduinoJOystickLibrary8axis repository to be added as a library to the scetch. 
